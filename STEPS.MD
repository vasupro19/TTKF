# creating global state
step 1: create slice inside `src/app/store/slices/yourSlice.js`
step 2: import and register slice inside `src/app/store/reducers.js`

# modify theme
modify files inside `src/core/theme`